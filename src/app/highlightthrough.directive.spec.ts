import { HighlightthroughDirective } from './highlightthrough.directive';

describe('HighlightthroughDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightthroughDirective();
    expect(directive).toBeTruthy();
  });
});
